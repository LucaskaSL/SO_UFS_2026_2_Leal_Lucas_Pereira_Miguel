services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3000:8080"
    volumes:
      - open-webui:/app/backend/data
    extra_hosts:
      - host.docker.internal:host-gateway
    environment:
      - WEBUI_SECRET_KEY=156164
      - OLLAMA_BASE_URL=http://172.26.11.126:11434
    restart: unless-stopped

volumes:
  open-webui:
